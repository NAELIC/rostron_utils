from setuptools import setup

setup(
    name='rostron_utils',
    author='NAELIC',
    description='library for rostron_navigation',
    packages=['utils'],
    long_descriptio=open('README.md').read()
)
